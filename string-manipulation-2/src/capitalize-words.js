/* exported capitalizeWords */
function capitalizeWords(string) {
  // let words = '';
  // for (let i = 0; i < string.length; i++) {
  //   if (string.charAt(i) !== 1) {
  //     words = string.charAt(0).toUpperCase() + string.slice(1).toLowerCase();
  //   }
  // }
  // return words;
}
